services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: grandt
      POSTGRES_PASSWORD: grandt
      POSTGRES_DB: grandt
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
